language : node_js
node_js :
- stable

branches:
only:
- master

before_script:
- npm install -g --silent firebase-tools
- npm install -g --silent @angular/cli
- npm install

script:
- ng build --prod
