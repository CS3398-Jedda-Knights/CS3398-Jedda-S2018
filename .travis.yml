language : node_js
node_js :
   - stable

branches:
   only:
      - master

before_script:
   - cd frontend
   - npm install -g --silent firebase-tools
   - npm install -g --silent @angular/cli

script:
   - ng build --prod

after_script:
   - cd ..
